# Copy this file to .env and fill in real values.
# .env is gitignored — .env.example is safe to commit.

# Home Assistant long-lived access token
# Generate at: HA → Profile → Long-lived access tokens
HA_TOKEN=
